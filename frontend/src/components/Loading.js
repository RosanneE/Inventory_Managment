//imports

//Loading Animation (used in Color and ColorsList)
const Loading = (props) => {
    return (
        <div>
            <p></p>
            <div className="loading">
                <h2>Loading</h2>
            </div>
        </div>
    )
}

export default Loading